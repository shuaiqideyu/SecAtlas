# DNS/DNSSEC 授权渗透攻击面矩阵

每个攻击面只保留一个最低影响验证路径；任何需要扩大范围的下一步都必须重新取得授权。

## 1. RDAP 与 DNS 被动枚举

- 核心目标：在不改变目标状态的前提下建立域名、注册数据、委派、关键记录和解析角色清单。
- 最小动作：dig +noall +answer "$ZONE" NS
- 预算：每个区最多 12 次 DNS 查询；同一名称/类型只查询一次，速率不高于每秒 1 次。
- 关键 Oracle：枚举结果只证明公开攻击面存在，不自动构成漏洞。
- 停止：达到资产与角色清单完整、出现 429/限流、超出授权域名或无法确认数据来源时停止。
- Runbook：[打开](./Runbooks/RDAP与DNS被动枚举.md)

## 2. 委派、NS 与胶水一致性

- 核心目标：判断父区委派、胶水、子区权威和 DNSSEC DS 是否形成一致且可解释的授权链。
- 最小动作：dig +norecurse @"$PARENT_AUTH" "$ZONE" NS
- 预算：每个父/子权威每种记录 1 次，整区最多 12 次查询。
- 关键 Oracle：胶水仅用于到达权威，不等于权威区内 A/AAAA 的最终真实性。
- 停止：发现超出授权的权威、节点开始限流、或无法取得变更窗口信息时停止并标记待补证。
- Runbook：[打开](./Runbooks/委派_NS与胶水.md)

## 3. AXFR/IXFR 区域传送边界

- 核心目标：用一次授权 TCP 请求验证区域传送是否只向预期二级权威开放。
- 最小动作：dig +tcp @"$AUTH" "$ZONE" AXFR
- 预算：每台权威 AXFR 1 次；可选 IXFR 1 次；无重试、无并发。
- 关键 Oracle：REFUSED/NOTAUTH/认证失败支持边界有效，但仍需配置证据确认预期。
- 停止：返回第一份完整区、出现连接异常、数据包含未预期敏感记录或授权身份不明确时立即停止。
- Runbook：[打开](./Runbooks/AXFR与IXFR.md)

## 4. RFC 2136 动态更新与 TSIG

- 核心目标：验证动态更新入口是否有事务认证、细粒度更新策略、审计与可回滚边界。
- 最小动作：只在隔离实验区：nsupdate -v -k "$EPHEMERAL_TSIG_KEY"
- 预算：生产成功路径 0 次；隔离实验空更新 1 次；失败不重试。
- 关键 Oracle：未认证请求被 REFUSED/NOTAUTH 支持安全边界。
- 停止：发现任何真实 RR 变化、serial 异常推进、密钥来源不明或日志泄露密钥材料时停止。
- Runbook：[打开](./Runbooks/动态更新与TSIG.md)

## 5. 公开递归与单响应尺寸

- 核心目标：在不制造负载或反射的前提下判断服务角色、递归访问控制和一次响应的尺寸边界。
- 最小动作：dig +recurse +stats @"$SERVER" "$IN_SCOPE_NAME" A
- 预算：每端点 2 次单查询；每秒不超过 1 次；禁止 ANY、DNSKEY 批量和 EDNS 尺寸扫测。
- 关键 Oracle：RA=1 本身不证明向任意公网源开放，需结合源 ACL 和实际成功递归。
- 停止：首个成功递归证据或首个拒绝对照完成即停；出现超时、限流或异常 CPU 信号时停。
- Runbook：[打开](./Runbooks/公开递归与单响应尺寸.md)

## 6. DNSSEC 验证状态与父子错配

- 核心目标：沿信任链区分 Secure、Insecure、Bogus 与 Indeterminate，并定位 DS、DNSKEY、RRSIG 或信任锚问题。
- 最小动作：dig +dnssec +noall +answer "$ZONE" DS
- 预算：每记录类型每权威 1 次，整条验证链最多 10 次查询。
- 关键 Oracle：Secure 需要从信任锚到目标 RRset 的完整成功验证。
- 停止：已定位到单一可复现链条断点、来源开始限流或无法证明信任锚时停止。
- Runbook：[打开](./Runbooks/DNSSEC验证与错配.md)

## 7. 缓存伪造前置条件与被动防护观察

- 核心目标：不发送伪造应答，仅审查离路径伪造所需前置条件及解析器的匹配、随机化、Cookie 和 DNSSEC 控制。
- 最小动作：dig @"$LAB_RESOLVER" entropy.signed.test A +stats
- 预算：生产 1 次正常查询；隔离统计使用固定 32 条夹具且不出网。
- 关键 Oracle：端口或事务 ID 的少量样本只能形成风险线索，不能证明可预测。
- 停止：获得版本/配置/正常流量三类证据或需要发送任何伪造数据包时停止。
- Runbook：[打开](./Runbooks/缓存伪造前置条件.md)

## 8. 隔离 DNS 重绑定前置条件

- 核心目标：只在 .test 与保留地址中验证名称两阶段切换，并评估浏览器、代理或服务端是否错误信任解析结果。
- 最小动作：dig @"$LAB_AUTH" switch.rebind.test A +noall +answer
- 预算：每阶段 1 次 DNS 查询；不向解析结果发起真实 HTTP 请求。
- 关键 Oracle：DNS 地址切换只证明前置条件，不证明应用可被利用。
- 停止：观察到两阶段解析、任何请求将触达非保留地址、或需要降低真实安全控制时停止。
- Runbook：[打开](./Runbooks/隔离DNS重绑定.md)

## 9. 悬空记录与子域接管证据

- 核心目标：区分“DNS 指向不存在资源”“提供商允许重新绑定”和“已具备授权影响”三段证据，不认领第三方资源。
- 最小动作：dig +noall +answer "$NAME" CNAME
- 预算：每个名称每类型 1 次 DNS 查询；可选 1 次 HEAD/状态观察；不创建资源。
- 关键 Oracle：只有悬空 DNS：标记配置异常，不写成可接管。
- 停止：下一步需要注册、付费、创建租户、修改 DNS 或接触第三方内容时停止。
- Runbook：[打开](./Runbooks/悬空记录与子域接管证据.md)

## 10. 加密 DNS 可见性与合成隧道检测

- 核心目标：理解 DoT/DoH/DoQ 对旁路观测的影响，并用无数据外传的合成流量验证多信号隧道检测。
- 最小动作：dig @"$LAB_RESOLVER" release-2026-07-20-build-artifact.cdn.test TXT
- 预算：每个合成对照 1 次；不编码真实数据、不循环、不发送到公网权威。
- 关键 Oracle：加密会话旁路只能证明端点、时序和大小，不应声称看到 QNAME。
- 停止：需要解密未授权流量、收集真实内容、制造持续信道或影响生产解析时停止。
- Runbook：[打开](./Runbooks/加密DNS可见性与合成隧道.md)
