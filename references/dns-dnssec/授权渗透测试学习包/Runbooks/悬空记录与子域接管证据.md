---
id: TECH-2026-5409
title: "悬空记录与子域接管证据"
kind: technique
track:
  - 授权渗透测试
platform:
  - 网络与协议安全
techniques:
  - DNS
  - DNSSEC
lifecycle:
  - 侦察
  - 验证
  - 复测
standards:
  - WSTG-CONFIG-10
  - RFC-1034
  - RFC-1035
source_id:
  - SRC-2026-5422
  - SRC-2026-5446
  - SRC-2026-5402
  - SRC-2026-5403
authorization: owned-authorized
confidence: verified
sensitivity: public
license: CC BY-SA 4.0
---

# 悬空记录与子域接管证据

## 目标

区分“DNS 指向不存在资源”“提供商允许重新绑定”和“已具备授权影响”三段证据，不认领第三方资源。

## 授权范围

- 仅检查授权子域及其公开 CNAME/NS/A/AAAA/MX/TXT。
- 不得注册域名、创建云资源、绑定自定义域或修改目标 DNS。
- 社区指纹只是候选，提供商行为必须按当前官方文档核对。

## 前置条件

- 授权域名清单
- 当前 DNS 响应
- 业务所有者/提供商资源台账

## 被动优先步骤

- 确认记录链最终目标及其 NXDOMAIN、SERVFAIL 或应用错误。
- 核对提供商是否强制域名所有权验证。
- 向业务所有者确认资源是否计划停用、迁移或暂时不可用。
- 检查同类记录和变更历史，避免仅凭单次 HTTP 指纹。

## 最小主动验证

```bash
dig +noall +answer "$NAME" CNAME
dig +noall +answer "$CNAME_TARGET" A AAAA
```

**硬上限**：每个名称每类型 1 次 DNS 查询；可选 1 次 HEAD/状态观察；不创建资源。

## 必备证据

- DNS 链
- 目标资源不存在或未配置证据
- 提供商当前所有权校验规则
- 资产台账

## 证据 Oracle

- 只有悬空 DNS：标记配置异常，不写成可接管。
- 悬空 DNS 加可重新绑定的独立证据：可标记高置信风险，但仍不认领。
- 提供商禁止他人绑定或资源仍归原租户：不是可接管。

## 常见误报

- 暂时停机
- 仍保留的租户
- 提供商错误页变更
- 需要 DNS 验证的安全绑定
- CDN 回源故障

## 停止点

下一步需要注册、付费、创建租户、修改 DNS 或接触第三方内容时停止。

## 清理

无资源创建；删除临时 HTTP body，仅保留状态、头部和脱敏截图摘要。

## 修复

- 先删除 DNS 再释放资源
- 为自定义域启用持续所有权验证
- 建立资源销毁与 DNS 清理联动

## 复测

- 旧记录不再解析到悬空目标
- 提供商拒绝未经验证绑定
- 业务预期名称仍正常

## 结论模板

- 观察事实：
- 支持的假设：
- 被排除的解释：
- 尚缺证据：
- 风险与影响：
- 已触发停止点：
- 修复后复测结果：
