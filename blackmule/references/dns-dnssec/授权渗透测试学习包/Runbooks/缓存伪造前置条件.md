---
id: TECH-2026-5407
title: "缓存伪造前置条件与被动防护观察"
kind: technique
track:
  - 授权渗透测试
platform:
  - 网络与协议安全
techniques:
  - DNS
  - DNSSEC
lifecycle:
  - 侦察
  - 验证
  - 复测
standards:
  - RFC-5452
  - RFC-7873
  - RFC-9018
  - RFC-3833
source_id:
  - SRC-2026-5307
  - SRC-2026-5308
  - SRC-2026-5309
  - SRC-2026-5439
authorization: owned-authorized
confidence: verified
sensitivity: public
license: CC BY-SA 4.0
---

# 缓存伪造前置条件与被动防护观察

## 目标

不发送伪造应答，仅审查离路径伪造所需前置条件及解析器的匹配、随机化、Cookie 和 DNSSEC 控制。

## 授权范围

- 生产仅做配置、版本、正常查询抓包和日志审查。
- 禁止猜事务 ID、抢答、伪造源、诱导重复查询或打满缓存。
- 统计性实验只在无公网出口的自有递归器上进行。

## 前置条件

- 控制递归器或有明确抓包授权
- 确认 NAT 路径
- 准备 .test 权威基线

## 被动优先步骤

- 核对解析器版本与应答匹配策略。
- 从正常流量观察事务 ID、源端口、目标权威和 DNS Cookie 支持。
- 检查 NAT 是否压缩或规律改写源端口。
- 确认可验证分支启用 DNSSEC，且 Bogus 不会静默降级。

## 最小主动验证

```bash
dig @"$LAB_RESOLVER" entropy.signed.test A +stats
# 仅分析本学习包 capture.pcapng，不发送伪造响应
```

**硬上限**：生产 1 次正常查询；隔离统计使用固定 32 条夹具且不出网。

## 必备证据

- 正常查询 PCAP
- 解析器与 NAT 配置
- Cookie 协商日志
- DNSSEC 验证行为

## 证据 Oracle

- 端口或事务 ID 的少量样本只能形成风险线索，不能证明可预测。
- DNS Cookie 只提高离路径伪造门槛，不抵御路径上攻击者。
- DNSSEC 对已签名数据提供根因级验证，但未签名分支仍需其它控制。

## 常见误报

- 抓包看到校验和卸载异常
- NAT 合法复用
- 测试解析器禁用 Cookie 但有 DNSSEC
- 缓存命中导致无上游查询

## 停止点

获得版本/配置/正常流量三类证据或需要发送任何伪造数据包时停止。

## 清理

销毁临时抓包中的客户端标识；实验缓存清空或随 VM 删除。

## 修复

- 升级解析器
- 保持大源端口空间与严格匹配
- 启用可用的 DNS Cookie 和 DNSSEC 验证
- 修正 NAT

## 复测

- 同一正常查询仍成功
- 配置和日志显示控制启用
- 隔离 Bogus 数据被拒绝

## 结论模板

- 观察事实：
- 支持的假设：
- 被排除的解释：
- 尚缺证据：
- 风险与影响：
- 已触发停止点：
- 修复后复测结果：
