---
id: TECH-2026-5401
title: "RDAP 与 DNS 被动枚举"
kind: technique
track:
  - 授权渗透测试
platform:
  - 网络与协议安全
techniques:
  - DNS
  - DNSSEC
lifecycle:
  - 侦察
  - 验证
  - 复测
standards:
  - RFC-9082
  - RFC-9083
  - RFC-9499
source_id:
  - SRC-2026-5419
  - SRC-2026-5420
  - SRC-2026-5421
  - SRC-2026-5302
authorization: owned-authorized
confidence: verified
sensitivity: public
license: CC BY-SA 4.0
---

# RDAP 与 DNS 被动枚举

## 目标

在不改变目标状态的前提下建立域名、注册数据、委派、关键记录和解析角色清单。

## 授权范围

- 只查询授权清单中的域名及其父区公开数据。
- RDAP 返回的联系方式可能被脱敏；不得尝试绕过差异化访问控制。
- 历史解析、CT 或搜索结果只能作为候选线索，必须重新核对当前 DNS。

## 前置条件

- 保存授权域名、允许查询时段和最大查询数。
- 区分注册数据服务、父区委派、权威数据和递归缓存。
- 为每个观察记录时间、解析器和查询类型。

## 被动优先步骤

- 从 IANA/ICANN 入口确定 RDAP 服务，不拼接未知私有端点。
- 记录 registrar、注册状态和事件时间；不把隐私遮蔽字段当作异常。
- 查询 NS、SOA、DS、A/AAAA、CNAME、MX 和 CAA 的公开值，并标记 TTL。
- 把通配符、CDN、Anycast 和分裂视图列为待验证解释。

## 最小主动验证

```bash
dig +noall +answer "$ZONE" NS
dig +noall +answer "$ZONE" SOA
dig +dnssec +noall +answer "$ZONE" DS
```

**硬上限**：每个区最多 12 次 DNS 查询；同一名称/类型只查询一次，速率不高于每秒 1 次。

## 必备证据

- 原始响应、解析器地址、时间、TTL 和响应标志。
- RDAP 事件字段及其公开/脱敏状态。
- 父区 NS/DS 与子区 SOA 的关联图。

## 证据 Oracle

- 枚举结果只证明公开攻击面存在，不自动构成漏洞。
- 父区与子区数据不一致时先排除缓存、计划轮换和多提供商切换。
- 只有后续 Runbook 的独立证据满足时才升级为具体发现。

## 常见误报

- CDN 多地址轮换
- 通配符 DNS
- 隐私代理或 RDAP 字段遮蔽
- 分裂视图

## 停止点

达到资产与角色清单完整、出现 429/限流、超出授权域名或无法确认数据来源时停止。

## 清理

不创建账号、不保存受限联系人字段；清除临时 HTTP 缓存，仅保留脱敏证据。

## 修复

- 清理未知记录和过期委派
- 建立域名资产台账与变更告警
- 为关键控制面启用抗钓鱼 MFA

## 复测

- 重新生成同类型清单
- 确认未知记录已删除或有业务归属
- 核对父子区委派一致

## 结论模板

- 观察事实：
- 支持的假设：
- 被排除的解释：
- 尚缺证据：
- 风险与影响：
- 已触发停止点：
- 修复后复测结果：
